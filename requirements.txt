# AI Overseer Market Analyzer Requirements
# Compatible with Python 3.11 for Render deployment

# Core dependencies with fixed versions for compatibility
Flask==3.0.0
flask-cors==4.0.0
werkzeug==3.0.0

# OpenAI - using newer version that's compatible
openai==1.6.1

# Essential packages
numpy==1.24.3
requests==2.31.0
python-dotenv==1.0.0

# Production server
gunicorn==21.2.0

# JSON handling (built-in, but ensuring compatibility)
setuptools==69.0.2
wheel==0.42.0
